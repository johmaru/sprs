pkg test;

pub fn test() >> str {
            var a = 5 - 1;
            var b = 10;
            var c = "hello" + " world";

            println!(c);

            # test equality

            if a == 4 then {
                return c;
            }

            if a != 4 then {
                return a++;
            } else {
                return a + 2;
            }

            return c;
        }