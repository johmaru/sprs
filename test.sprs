pkg test;

fn test() {
            var a = 5 - 1;
            var b = 10;
            var c = "hello" + " world";
            println!(c);

            # test equality
            if a == 3 then {
                return a;
            }

            if a != 3 then {
                return a++;
            } else {
                return a + 2;
            }

            return b;
        }